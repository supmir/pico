<div class="">
  <h1 class="font-bold text-4xl">
    <%= @link.title %>
  </h1>
  <a href="<%= @link.href %>">
    <%= @link.href %>
  </a>

  <div>

    <% @link.shortened_links.each do |shortened_link| %>
      <div class="flex">
        <a href="/<%=shortened_link.path%>"><%=shortened_link.path%></a>
        <a href="/picos/<%= shortened_link.id %>">View Report</a>
      </div>
    <% end %>

  </div>
</div>
